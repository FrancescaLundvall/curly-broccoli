name: CI

on:
  push:
  pull_request:

concurrency:
  group: ${{ github.workflow }}

jobs:
  node-tests:
    name: 'Tests'
    runs-on: ubuntu-24.04

    steps:
      - name: Check out code
        uses: actions/checkout@v6

      - name: Setup Node.js environment
        uses: actions/setup-node@v4
        with:
          node-version-file: '.nvmrc'
          registry-url: 'https://registry.npmjs.org'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      # Run linter in CI, but do not run with --fix
      # This would hide any actual errors found by the linter
      - name: Run linter
        run: npx eslint "{src,apps,libs,test}/**/*.ts"

      - name: Run tests
        run: |
          npm run test
          npm run test:e2e
